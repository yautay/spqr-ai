Legions - NEXT STEPS for next AI session

Current baseline
- Milestones 0 through 5 are complete in backend/frontend:
  - shared schema package and CI baseline checks,
  - turn/activation flow with phase-gated actions,
  - per-step domain events and websocket streaming,
  - snapshot save/load APIs with frontend controls,
  - replay reconstruction and deterministic replay verification tests.
- Full quality gate currently passes via `just check`.

Remaining scope
1) Milestone 6 desktop packaging only:
   - add Electron shell app,
   - run local backend process from shell,
   - package desktop build,
   - validate Tauri-neutral frontend,
   - prototype Tauri shell compatibility.

Coding constraints
- Keep files small and strongly typed.
- Keep deterministic behavior in core rules, AI, and replay pipeline.
- Add tests for every behavior change.
- Keep line length <= 140 chars.

Suggested first task next session
- Scaffold desktop shell (`apps/desktop-electron`) and implement backend process lifecycle management.
