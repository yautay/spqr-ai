Legions - NEXT STEPS for next AI session

Current baseline
- Milestone 3 UI slice is complete in `apps/web`.
- PixiJS battlefield rendering is live with terrain hexes, unit layer, hover/selection states, and legal-move highlights.
- Frontend can execute move, missile, reload, and shock actions against existing REST contracts.
- Frontend action preview panel shows move path/MP preview and latest action breakdown metadata.
- Event log panel normalizes action outcomes/events into user-facing timeline entries.
- Backend exposes `GET /game/legal-moves/{unit_id}` with deterministic path + cost metadata for UI overlays.
- Full quality gate is green via `just check`.

Priority order
1) Start Milestone 4 AI slice: legal move generator integration entrypoint
2) Add backend read-only AI evaluation endpoint scaffolding (state feature extraction)
3) Add shallow search prototype with strict move time budget
4) Emit AI explanation payload for chosen action
5) Add AI-vs-AI smoke simulation command and baseline regression test

Coding constraints
- Keep files small and strongly typed.
- Keep deterministic behavior in core rules and AI action selection.
- Add tests for every behavior change.
- Keep line length <= 140 chars.

Suggested first task next session
- Implement AI-facing legal action enumeration over current move/missile/shock action contracts.
