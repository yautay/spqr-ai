Legions - NEXT STEPS for next AI session

Current baseline
- Typed parser and loader for rules tables are implemented.
- API startup validates and loads all currently supported rules tables.
- Movement resolution uses table-driven terrain MP profiles selected by ruleset.
- Movement baseline data exists in `movement_costs.json`; elevation/facing values are still placeholders.
- Movement pathfinding supports occupied-hex pass-through checks via voluntary stacking table categories.
- Game state supports multi-unit occupancy and voluntary stacking stop-in-hex when table rows allow it.
- Movement applies voluntary/mandatory stacking side effects and emits stacking effect metadata with TQ-check fields.
- Movement action results include pending TQ checks and resolved TQ outcomes with rolled value (`roll`).
- TQ and missile rolls use seeded per-game RNG state (`rng_seed` + advancing `rng_counter`) for deterministic replayable sequences.
- Failed TQ checks apply cohesion loss and set stationary units to routed state (`is_routed`).
- Routed-unit movement uses mandatory stacking lookup (with `routing_*` moving categories).
- Stacking table JSONs are finalized for current category matrix and loader validates complete pair coverage.
- Missile range/results table and DR modifiers are finalized and consumed by runtime resolver.
- Missile resolution supports active/reaction fire payload modes, DR-modifier breakdown, supply depletion, and reload attempts.
- Movement and missile API contracts are covered by smoke tests and stable for UI integration.

Priority order
1) Implement LOS runtime + integrate LOS validation into missile resolver
2) Auto-apply state-derived missile DR modifiers (terrain/supply) in core resolver
3) Add movement-driven reaction-fire windows (entry/retire/return) and single-window consumption rules
4) Add minimal turn-phase model and gate reload to rout/reload phase
5) Fill next Original parity tables (rally, leader casualty, shock)

Data import workflow
- Source files are in `docs/sources/charts/` and `docs/SPQR_4thEdition_Living_Rules_Draft_Oct2022.pdf`.
- For each table:
  - copy `*.template.json` -> `<table_id>.json`
  - fill values, keep schema keys stable
  - keep `null` only where value is unknown and add short note
- Validate with `just check` from repo root.

Coding constraints
- Keep files small and strongly typed.
- Keep deterministic behavior in core rules.
- Add tests for every behavior change.
- Keep line length <= 140 chars.

Suggested first task next session
- Implement LOS resolver and connect it to `resolve_missile` with tests for blocked/unblocked lines.
