# Shared Schema

Shared TypeScript contracts consumed by the frontend.

Source of truth remains backend Pydantic schemas in `apps/api/src/legions_api/api/schemas.py`.

The package also stores generated OpenAPI output (`openapi.json`) for contract checks.
